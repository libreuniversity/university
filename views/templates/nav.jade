nav
  a.brand(href="/") Libre University

  // responsive
  input#bmenu.show(type="checkbox")
  label.burger.pseudo.button(for="bmenu")
    i.icon-menu

  div.menu
    a.pseudo.button.icon-graduation-cap(href="/subject") Asignaturas
    if user
      a.button.icon-user(href="/user/" + user._id)= user.email
    else
      label.button.icon-users(for="login") Entrar
